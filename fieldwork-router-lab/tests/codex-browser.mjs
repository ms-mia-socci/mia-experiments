import {chromium} from 'playwright';import assert from 'node:assert/strict';
const browser=await chromium.launch();try{const page=await browser.newPage({viewport:{width:1440,height:1000}});await page.goto('http://127.0.0.1:5373');await page.getByRole('button',{name:/Mia Socci/}).click();await page.locator('.start-page').waitFor();await page.waitForLoadState('networkidle');await page.getByRole('button',{name:/ENGINEERING TASKS.*OpenAI Codex/i}).click();await page.getByPlaceholder('Message OpenAI Codex…').fill('Respond with exactly REGIONAL_UI_OK. Do not use tools.');await page.getByRole('button',{name:'Submit',exact:true}).click();await page.waitForFunction(()=>document.querySelector('.run-status')?.textContent==='Ready'&&document.querySelector('.chat-messages')?.textContent?.includes('REGIONAL_UI_OK'),{},{timeout:90000});const text=await page.locator('.chat-messages').innerText();assert.match(text,/OpenAI Codex[\s\S]*REGIONAL_UI_OK/);await page.screenshot({path:'.local/codex-regional.png',fullPage:true});console.log('Codex browser AG-UI streaming passed');}finally{await browser.close();}
